# 预览海报对齐 UI 验证记录

验证日期：2026-07-13

## 验证范围

- 首页海报与透明交互热区。
- 答题页动态题目、五个答案、选中反馈与上一题入口。
- 结果页动态结果、四轴评分、说明区和首屏行动入口。
- 分享卡预览与保存、重生成、复制链接操作区。

## 环境

- 本地地址：`http://127.0.0.1:5173`
- 浏览器：Playwright Chromium（用户确认使用本地 Playwright 作为内置浏览器超时后的回退）
- 视口：375 × 812、390 × 844、430 × 932、1280 × 900

## 截图证据

- `output/playwright/home-375x812.png`
- `output/playwright/quiz-375x812.png`
- `output/playwright/home-390x844.png`
- `output/playwright/quiz-390x844.png`
- `output/playwright/result-430x932.png`
- `output/playwright/share-430x932.png`
- `output/playwright/home-1280x900.png`

## 结果

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 首页与预览海报一致 | 通过 | 375、390 和桌面截图均显示完整海报，按钮热区可点击 |
| 答题页无固定文案重影 | 通过 | 中心内容板使用实底，截图只显示动态题目 |
| 五个答案和上一题位于首屏 | 通过 | 375 与 390 的页脚底部坐标均为 693，小于对应视口高度 |
| 答案交互推进 | 通过 | 点击 NPC 后题号由 01 / 24 进入 02 / 24 |
| 结果页主操作位于首屏 | 通过 | 430 视口行动区底部坐标为 885，小于 932 |
| 分享面板无幽灵按钮 | 通过 | 中心内容板使用实底，仅显示三项真实操作 |
| 横向溢出 | 通过 | 四个视口的 `scrollWidth` 均未超过 `clientWidth` |
| 桌面电话画布 | 通过 | 1280 视口中画布宽 430，左偏移 425，严格居中 |
| 控制台健康 | 通过 | 0 errors，0 warnings |

## 自动化门禁

- `npm test -- --run`：14 项测试通过。
- `npm run typecheck`：通过。
- `npm run build`：通过。

## 剩余风险

- 未覆盖 Safari 和 Firefox 的字体栅格化差异。
- 分享卡 PNG 的真实下载行为由 Vitest mock 覆盖，本轮浏览器验收只验证了分享面板与操作控件渲染。
