# 首发测试门禁报告

日期：2026-07-01

角色：测试负责人 / 首发测试门禁

项目：`D:\javaProjects\personal\longhair-guy-detector`

本地预览：`http://127.0.0.1:5173/`

基线提交：`3b281bd docs(角色): 记录架构复核分享卡返工通过`

## 结论

测试结论：通过。

本轮未发现阻塞首发测试门禁的问题。建议进入 QA 对抗式验收和运维 / Cloudflare Pages 发布前门禁；本报告不包含生产部署验证，也未执行 Cloudflare 发布。

非阻塞风险：

- 本轮验证基于本地 Vite 预览服务，未验证 Cloudflare Pages 真实生产 URL。
- 内置 Browser 插件已用于页面身份、非空、基础交互和 console 健康烟测；精确 390x844 / 430x932 截图和 PNG 下载证据以普通 Playwright + 本机 Chrome 为准。
- 网络请求捕获脚本首次运行时因临时脚本未等待题号推进而超时；按系统化调试确认是验证脚本节奏问题，未改业务代码，修正等待条件后复跑通过。

## 范围

已覆盖：

- `npm test -- --run`
- `npm run typecheck`
- `npm run build`
- 首页 -> 答题 -> 结果 -> 分享卡流程
- 全 NPC、强正、强负路径抽查
- 16 个结果码可达性依据和分享卡渲染遍历
- 390x844、430x932 分享卡移动端无横向溢出
- 保存 PNG 为 1080x1350
- 复制结果链接可打开
- console / pageerror 为空
- 首发边界：不登录、不上传答案、不采集个人身份信息、不包含广告 / 付费 / 统计 SDK / 浏览器端模型 API / 凭据

未覆盖：

- Cloudflare Pages 线上部署、回滚、真实生产 URL 访问。
- 多浏览器兼容矩阵；本轮普通 Playwright 使用本机 Chrome。
- 线上 CDN / 缓存行为。

## 自动化命令

| 命令 | 结果 | 证据摘要 |
| --- | --- | --- |
| `npm test -- --run` | 通过 | Vitest `3` 个测试文件、`11` 项测试通过；包含 24 个 NPC 答案进入分享卡结果视图 |
| `npm run typecheck` | 通过 | `tsc -b` exit 0 |
| `npm run build` | 通过 | Vite `v6.4.3`，`1589` modules transformed，`dist/index.html`、CSS、JS 产物生成成功 |

## 评分核心

源码和测试依据：

- `src/domain/scoring.test.ts`
- `src/data/results.test.ts`
- `src/App.test.tsx`
- `src/domain/scoring.ts`

验证结果：

- 全 NPC 答案：自动化测试和浏览器流程均稳定得到 `OHBG`。
- 强正路径：浏览器流程全选第 5 档，得到 `AHBG`。
- 强负路径：浏览器流程全选第 1 档，得到 `OTDF`。
- 16 个结果码可达：`src/domain/scoring.test.ts` 使用 `answersForResultCode(code)` 遍历 `RESULT_CODES`，断言每个结果码都可命中。
- 16 个结果数据完整：`src/data/results.test.ts` 断言 16 个结果码顺序、短名、渲染关键字段、ATDF 风险词边界和全局文案池。

## 浏览器验证

### Browser 插件烟测

Browser 插件用于基础页面身份和交互烟测：

- URL：`http://127.0.0.1:5173/`
- title：`长发男鉴定器`
- DOM 非空，包含 `长发男鉴定器`
- `开始鉴定` 按钮唯一
- 点击后进入 `01 / 24`
- console error / warn：`0`
- Vite / framework error overlay：未发现

### Playwright 完整门禁

执行环境：

- 普通 Playwright
- 本机 Chrome：`C:\Program Files\Google\Chrome\Application\chrome.exe`
- 证据目录：`C:\Users\12156\AppData\Local\Temp\longhair-guy-detector-test-gate-2026-07-01`

流程结果：

| 路径 | 期望 | 实际 | 结果 URL |
| --- | --- | --- | --- |
| 全 NPC | `OHBG` | `OHBG` | `http://127.0.0.1:5173/?result=OHBG` |
| 全强正 | `AHBG` | `AHBG` | `http://127.0.0.1:5173/?result=AHBG` |
| 全强负 | `OTDF` | `OTDF` | `http://127.0.0.1:5173/?result=OTDF` |

截图和下载产物：

| 文件 | 尺寸 | 字节数 | 说明 |
| --- | --- | ---: | --- |
| `home-390x844.png` | 390x844 | 149887 | 首页移动端截图 |
| `quiz-390x844.png` | 390x844 | 127187 | 第 1 题答题页截图 |
| `result-share-390x844.png` | 390x844 | 99032 | 390x844 分享卡预览截图 |
| `result-share-430x932.png` | 430x932 | 105970 | 430x932 分享卡预览截图 |
| `share-card-download.png` | 1080x1350 | 728202 | 保存 PNG 下载文件 |

分享卡度量：

| 视口 | 页面溢出 | 卡片溢出 | 面板溢出 | 文案重叠 | 关键度量 |
| --- | --- | --- | --- | --- | --- |
| 390x844 | 否 | 否 | 否 | 否 | `document.scrollWidth=390`，`.share-card 307/307`，`.share-panel 334/334` |
| 430x932 | 否 | 否 | 否 | 否 | `document.scrollWidth=430`，`.share-card 347/347`，`.share-panel 374/374` |

16 个结果码遍历：

- 视口：390x844
- 操作：逐一打开 `?result=<code>`，打开分享卡
- 检查：页面横向溢出、分享卡横向溢出、分享面板横向溢出、标题 / 场景 / 红色文案 / footer 重叠
- 结果：`16` 个结果码均通过，失败数 `0`

复制链接：

- 剪贴板文本：`http://127.0.0.1:5173/?result=OHBG`
- 新页面打开该链接后结果码：`OHBG`
- 结论：复制链接可用

console / pageerror：

- Playwright `consoleIssues`: `[]`
- Playwright `pageErrors`: `[]`

## 首发边界

源码 / 构建产物检查：

- `src/App.tsx` 答案状态保存在 React 内存状态中；结果分享只把 `result=<code>` 写入 URL。
- 未发现 `localStorage`、`sessionStorage`、`indexedDB`、cookie 写入。
- 仅发现 `navigator.clipboard.writeText` 用于复制结果链接，`history.replaceState` 用于结果 URL。
- 未发现登录、手机号、生日、精确位置、真实姓名、答案上传表单或服务端提交逻辑。
- 未发现广告、付费、购买诱导、统计 SDK、浏览器端模型 API、OpenAI / imagegen / image2 密钥或业务凭据。
- `public/_headers` 设置 `Permissions-Policy: camera=(), microphone=(), geolocation=()`。

网络请求捕获：

- 操作：本地打开首页，完成 24 个 NPC 答案，打开分享卡，触发保存 PNG。
- 请求总数：`19`
- 外部请求数：`0`
- 请求范围：仅 `http://127.0.0.1:5173/` 下的 Vite dev server 脚本、源码模块、依赖模块和 favicon。

关键词扫描说明：

- 构建 bundle 中存在 Vite 模块预加载和 `html-to-image` 内部资源内联使用的 `fetch`，本轮网络捕获未发现外部请求或答案上传。
- 页面中出现“心理诊断”“人格科学”均为否定边界表达，例如“不构成人格诊断”“不是心理诊断，也不是人格科学”。
- 未发现“精准识别”“精准测评”“权威测评”等正向包装。

## 角色与文件边界

遵守情况：

- 未修改 `src/**`。
- 未修改构建脚本或配置。
- 未部署 Cloudflare。
- 未提交 `dist/`、`.codegraph/`、`node_modules/`、截图临时目录或 `docs/design/v2/`。
- 本轮仅新增本测试报告，并更新 `.codex/role-windows.md` 测试状态和回调记录。
- 曾尝试运行 `agent-role-orchestrator` 的项目台账检查脚本，但当前 shell 中 `python` 为 Microsoft Store 占位符且 `py` 不可用；已改为人工核对台账字段和回调格式。

## 回调摘要

- 当前状态：测试门禁通过。
- 本轮变化：新增本测试报告；更新角色台账；复跑命令门禁、浏览器流程、分享卡、复制链接、隐私 / 网络边界。
- 证据文件 / 命令：本报告、`npm test -- --run`、`npm run typecheck`、`npm run build`、临时截图目录 `C:\Users\12156\AppData\Local\Temp\longhair-guy-detector-test-gate-2026-07-01`。
- 阻塞 / 风险：无阻塞；生产部署和 Cloudflare URL 仍需运维门禁。
- 下一回流对象：总控 `019f17ca-0d15-77a3-8d57-1905c94bf7bd`，抄送架构 `019f18f0-959a-75e0-8c8b-a47b19261fb0`。

## 技能命中回传

- 已加载并使用：`agent-role-orchestrator`、`test-case-report-builder`、`build-web-apps:frontend-testing-debugging`、`browser:control-in-app-browser`、`playwright`、`verification-before-completion`、`systematic-debugging`。
- 来源窗口要求但未使用：无。
- 临时发现应补用：`browser:control-in-app-browser`，因为前端测试技能要求 Browser 可用时优先使用。
- 误召 / 无效加载：无。
- 影响产出的 skill：角色编排约束了台账和回调；测试报告技能约束了证据完整性；前端测试和 Playwright 约束了真实浏览器、截图和交互验证；完成前验证约束了提交前复跑；系统化调试用于定位临时网络捕获脚本超时原因。

## 规则沉淀

- 可复用优化沉淀：无。
- 具体问题或优化：本轮未发现需要修改全局 skill、README 或项目 QA 清单的稳定问题。
